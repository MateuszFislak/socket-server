package com.interview.collibra.messageserver.command;

public enum Command {
    GREET_CLIENT,
    TERMINATE,
    ADD_NODE,
    ADD_EDGE,
    REMOVE_NODE,
    REMOVE_EDGE,
    UNKNOWN,
    FIND_SHORTEST_PATH
}
